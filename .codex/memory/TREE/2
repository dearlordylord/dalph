Codex built-in Memories are disabled; checked-in OptMem is advisory. Parallel root sessions may write only in master's shared primary worktree, where OptMem locks appends; other worktrees propose entries.                                                                                   
Milestone 1 correlates coarse same-process executor work by planned-attempt RunId and AttemptId and holds capacity until terminal or safe suspension; responsibility-began is coordinator-initiated, while executor reports are non-actions with no DalphExecutor actor.                       
Authored cassettes keep typed domain-outcome assertions in the story and document authored/recorded conformance laws; they use InitialControlPolicy at startup, while SetTaskExecutionCapacity may decode but stops unsupported until its production protocol is accepted.                     
2026-07-30 competitor scan: Gas Town+Beads is closest to graph-bounded restorable worktrees; Symphony is closest tracker scheduler; HerdOS is closest external-authority delivery loop. None specifies Dalph's exact Base SHA, multi-authority recovery, and accepted-head protocol.           
2026-07-30 reliability audit: focus open-source control planes; compare recovery, architecture, DI, verification, and user effects. Kandev leads local restoration; AMA admission is real but pruning unsafe; plain Effect helps now, while Workflow needs authority/crash evaluation.         
2026-07-31--08-01: Competitor failures are source-inferred; compare via atlas and architecture/evidence markers. #57 accepts explicit first-valid agent M with exact [H,C], bounded same-session correction, fail-closed correlation, and preserved non-convergence releasing target.          
2026-08-01: #134 Run Pause is a best-effort selection boundary: finish selected work, start no new progress/polling, fresh-read after Unpause. #65 uses one idempotent first-journaled exact-fingerprint choice; Stop proves quiescence and separately reconciles claim release.               
2026-08-01 Delivery activation uses a reconstructed graph and immutable frame. #135 Task Pause covers current grouping descendants; debt starts when coverage reaches a running attempt, survives Unpause/regrouping to settlement, and resumes through task-scoped fresh reads.               
2026-08-02: #177/#184 replaced checkedTurn with immutable proposal routes and cut all modes to literal flat delivery plus one scoped runtime; recovery is read-only, while action protocols own execution, admission, invalidation, and cleanup.                                               
