import { runActiveGraphRefresh } from "./controlled-characterization.js"

/** Maintained DS-20 continuation of the actual retained-C Journal and outside authorities. */
export const activeGraphRefreshCassetteCatalog = { activeGraphRefresh: { run: runActiveGraphRefresh } } as const
