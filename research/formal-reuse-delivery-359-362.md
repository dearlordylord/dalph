# Local formal reuse delivery evidence

A developer first verifies the integrated checkout, then runs the complete formal
profile or reuses its original stopped execution, and finally obtains that same
requirement during normal application handoff. This is the ordered #359 → #360 →
#361 → #362 feature, not four independent releases. The feature is not yet
qualified for handoff or integration.

The accepted behavior remains in [#358's approved specification](https://github.com/dearlordylord/dalph/issues/358#issuecomment-5649238628)
and [#357's command/evidence contract](https://github.com/dearlordylord/dalph/issues/357#issuecomment-5649197858).
All four issues and their blocking relationships remain authoritative. These
changes concern repository verification commands, files and processes; they do
not change Dalph's tracker, executor, Git integration or workflow journal behavior.

## Baseline and bounded seam evidence

The selected integration worktree is
`/workspace/typescript/dalph-worktrees/integration-formal-reuse-359-362`, on
`integrate/formal-reuse-359-362`, based on exact integrated master
`990148ff20b0636f2428210973b5b80920e5a2df`. The upstream fetch, ancestry check,
empty approved-reference diff and coherent frozen installation establish B1;
see [the #359 record](formal-reuse-readiness-359.md). No divergent root or
unrelated worktree was reset.

[Input seam evidence](formal-reuse-input-seam-360.md) records the finite-root
inventory, real observer/hash probe and external-library edit/revert refusal.
Its 18.238-second result is an experimental identity probe, not a complete warm
command measurement. [Owned-server evidence](formal-reuse-server-seam-360.md)
records one real reflection/check/stop probe in 4.618 seconds with ambient
listeners unchanged. These probes established implementation seams; neither is
the final F or R measurement required by #362.

## Scenario-to-test mapping

The following passing focused evidence belongs to #360. Later source changes
must rerun affected controls. No aggregate count substitutes for these outcomes.

| Scenario | Concrete outcome and focused evidence | Remaining owner |
| --- | --- | --- |
| B1/B2 | Exact baseline and installation provenance; failure stays a prerequisite, never permission for unrelated repair. Readiness report above. | #359 evidence complete |
| S1/S2 | `formal-gate.integration.test.mjs`: `records complete formal success only after obligations and terminal evidence then reuses the original without launches`; 105 real controlled checker receipts, one stopped server, original timestamp and zero warm launches. | #362 real final measurements |
| S3/S4 | `formal-input-policy.test.mjs`: membership/content/mode/link invalidation and unrelated-edit/HEAD/index/base independence. `quint-effective-profile.test.mjs`: independent complete manifest and substitution/omission rejection before launch. | #361 candidate/MBT handoff assertions |
| S5 | Input policy tests bind same-version installed bytes, environment presence/value, profile and policy; reject undeclared/cyclic/unreadable inputs. Pinned-lexer differential tests cover multiline and quoted import locators. Configuration tests observe and reject implicit Apalache configs. Owned-client tests bind exact Java and JVM home. | #362 complete-path negative controls |
| S6 | Dispatch integration proves missing optional logs retain reuse, while corrupted required verdict reports rerun all 105 commands. Evidence tests reject malformed/truncated/obsolete/mismatched records. Profile tests reject bare exits without verdict output. | #362 final evidence retention |
| S7 | Real-observer input tests reject edit/revert before initial identity, during guarded qualification and at final validation; implicit-config create/revert is rejected. Existing observer/resume suite proves overflow/watch-loss behavior remains fail-closed. | #361 external-tool observation through handoff |
| S8 | Dispatch tests prove same-worktree wait/reread, inherited admission without deadlock, and two linked worktrees sharing one clone slot while retaining separate full profiles, servers and records. | #361 actual nested quality caller |
| S9 | Owned-server tests prove exact listener/process ownership, ambient isolation, readiness failure and stopped custody. Client negative control detects the original automatic fallback; patched client refuses replacement/download. | #362 final prepared-installation execution |
| S10/S11 | Dispatch force-failure test proves old success cannot return. The publication crash matrix kills actual wrapper/coordinator processes before temp creation, between pointer temp fsync and rename, and after durable pointer publication. Genuine fences refuse relaunch; production reconciliation precedes full retry or original complete-evidence reuse. | #362 final qualification evidence |
| S13 | Actual dispatch refuses unresolved worktree custody before preparation/checkers; input and server tests refuse unsupported prerequisites before launch. | #362 full-path controls |
| S14 | `quint-ci-contract.test.ts` retains jobs/matrix/deadlines and hosted MBT exclusion while asserting explicit hosted formal routing. Profile controls preserve all 105 obligations and 42 scheduling/provenance steps against the immutable baseline. | #361 preservation during quality integration |
| S12 | Normal handoff must run/reuse after preflight, retain external-tool observation and perform final bounded no-checker validation even after prefix reuse. | #361 implementation pending |

## Scoped review closure

Standards/architecture review found no blocker in #360. The temporary
`validateOnly` API is explicitly assigned to #361's required final no-checker
caller; it must have that consumer before complete-feature closure. A possible
fixture/production toolchain type split was a judgment suggestion, not a
demonstrated violation: production obtains the shape only from its validating
resolver, and tests inject the controlled boundary deliberately.

Spec review found and closed three concrete gaps:

- A regular expression missed multiline/quoted Quint imports. The pinned lexer
  and file-source resolver now identify every import source, checked against
  the pinned parser grammar; corresponding controls
  pass.
- Apalache reads cwd/ancestor `.apalache.cfg` and actual JVM-home configuration.
  The resolver identifies JVM home, execution fixes it, and observation includes
  every implicit configuration's absence. Present unsupported configs fail;
  TLC uses the same identified Java/home. Focused controls pass.
- Synthetic record changes did not prove publication crashes. The real wrapper
  crash/reconciliation matrix above now proves the required before/during/after
  outcomes with real filesystem publication and process death.

Independent scoped reinspection reported both remaining blockers resolved on
input-policy hash `db5adc358993a35bf3924469be9e6e67602f5e877a36aeb84c61e3df410bab3a`,
integration-test hash `7841b33d4ed3a24a4cd546303c12474a38b243d3cf99cf1ee45b03c91d88fc34`,
and patch hash `45e9e1046beda69ccabbdc6c70e3ffb676e609797aae617d74896c39458b148c`.
Subsequent affected changes still require their scoped checks.

## Remaining qualification

#361 owns local quality integration, final revalidation, selector removal,
output-budget consolidation and accompanying command-policy documentation.
#362 owns the final frozen fresh formal success, ten unchanged warm samples,
phase measurements, justified production allowances, output accounting,
benefit-versus-maintenance decision, `pnpm check:all --candidate=<base-sha>` and
final `pnpm check:quint` applicability before integration. None is represented
as complete by the focused evidence above.

The current local wrapper allowances are explicitly provisional following the
bounded probes. They preserve 720-second execution, 750-second regression and
5+2-second termination/absence policy. No withdrawn timing estimate, provisional
five-second target or historical measurement is claimed as a release result.

## Assembled input guard qualification before #361

The changed production guard completed under the unchanged 53-second TERM
plus 7-second kill experimental watchdog: tool resolution 0.732458 seconds,
initial observation/fingerprint 15.878927 seconds, final validation 7.333116
seconds, total 23.944507 seconds. It recorded 216 source entries and 25,337
tool entries; the continuous observer was ready, drained and unchanged. No
checker or server launched. The retained raw result is
`.scratch/formal-reuse-probes/assembled-input-guard.json`.

The preceding assembled attempt timed out. Component measurements identified
full AST construction and serial asynchronous filesystem traversal as the
limiting phases. The successful version uses the pinned lexer for conservative
source discovery and the existing synchronous fingerprint style, retaining all
roots, complete hashes, deadline checks and edit/revert observation. No failed
benchmark was retried unchanged or granted a larger experimental watchdog.
This result is neither the final fresh F nor a warm R command measurement.

#361 now integrates S4/S8/S12/S14: after successful preflight, local handoff
obtains complete formal evidence under inherited admission, observes external
tools through application qualification, and performs final no-checker
validation even with a fully reused quality prefix. The test mapping adds
actual quality dispatch, external-tool edit/revert rejection, immutable
historical evidence and explicit hosted routing. Final qualification remains
owned by #362.

## #361 integration and review closure

The ordinary local handoff now obtains formal verification after successful
preflight, even when every application stage is credited from an eligible
prefix. The same external-tool observer stays active until final applicability
and the last candidate/formal drains. A final mismatch fails without rerunning
checkers. Explicit package purposes preserve hosted separation and prevent
local CI/lifecycle/argument shortcuts. Historical composites read their
original immutable formal success, independently of later attempts.

S4/S8/S12/S14 actual quality integration proves preflight before the one server
and 105 controlled checkers, stopped server before MBT, warm zero checker/server
launches with required MBT, full-prefix resume with current formal validation,
stale-pointer fresh execution, original historical evidence after later success,
post-formal mutation failure, and checker failure preventing MBT. The corrected
quality scenario passed in 60.88 seconds under concurrent controlled-test load.
Real-observer lifecycle tests additionally reject external library edit/revert
after formal completion and after the final snapshot.

Focused results: formal controls 90/90 in 14.62 seconds (inside the existing
60-second preflight slot), evidence/prefix controls 24/24, coverage explanation
42/42, changed-file controls 2/2, and scoped routing/CI/preflight checks pass.
The complete eight-case formal integration run passed seven cases and exposed
a misplaced historical assertion in the standalone corrupted-report case.
After removing that assertion and retaining it at the intended quality boundary,
both affected cases passed (105.22 seconds combined). The other seven-case run
results, including actual publication crash/reconciliation, remain applicable.

The documented bootstrap completed successfully; typecheck and changed-file
lint passed. Advisory Effect suggestions in unchanged application files are
not defects introduced by this tooling change. No full handoff has run yet.

Independent standards and spec review closed the unused validation API,
misleading changed-file wording, missing boundary/allowance documentation, and
missing actual formal-failure/MBT assertion. No production blocker remained.
The latest accepted allowance inventory is explicitly provisional at 106.5
minutes plus existing quality setup/termination overhead, below the existing
24-hour custody limit. #362 must validate the measured headroom and maintenance
assessment before release.

## Qualification corrections after the first committed candidate

The admitted preflight at `3fead985` failed four stages: full lint exceeded
300 seconds, resume controls exceeded 60 seconds, formal controls inherited
the outer Git base in disposable server repositories, and capability
registration exceeded 60 seconds. Candidate edits during the already-failed
census also make that run unproven; it is not passing handoff evidence.

Corrections remain in test fixtures: the server fixture supplies its own base;
the pnpm resume fixture now supplies independently validated formal records;
synthetic original record setup uses ordinary private writes before launch;
and three quality fixtures copy 30 runtime modules (233,290 bytes) instead of
all 158 scripts (1,138,621 bytes). Production observation, complete 105-command
proof, actual atomic publication and publication-crash tests are unchanged.
Independent scoped review found no weakening in those corrections.

After correction, formal controls passed 90/90 in 13.44 seconds with the outer
base inherited. The complete resume controls passed 148/148 in 43.05 seconds
under the unchanged 60-second cap. Earlier longer, overlapping or timed-out
runs are retained separately and are not represented as passing that cap.

The lint timeout remains unresolved. The unchanged native lint phase passed
on 1,007 files in 33.58 seconds; an isolated compatibility-lint invocation on
one unchanged TypeScript file exceeded a 53-second diagnostic watchdog.
Matching full-lint timeouts, and a capability timeout, were also recorded in
other active worktrees. This supports investigating the shared compatibility
phase rather than attributing the failure to the formal module graph, but
does not establish a root cause or waive final gates. No timeout was increased
and no failed benchmark was repeated unchanged.
